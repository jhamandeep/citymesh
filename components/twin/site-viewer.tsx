'use client';
import FullscreenButton from '@/components/twin/fullscreen-button';
import { useEffect, useRef, useState } from 'react';
import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { createStructure, createEquipmentMesh, createWiringFixtureMesh, disposeObject, loadSiteGlb } from '@/lib/site-geometry';
import type { Equipment } from '@/lib/site-model';
import {Checkbox} from '@/components/ui/checkbox';
import {createCableMesh} from '@/lib/cable-geometry';
import {cableLabels,type CablePlan,type CableMedium} from '@/lib/cabling';
import type { SiteType } from '@/lib/private-network';
import {applySurveyAlignment,setSurveyOverlay as setSurveyMaterialOverlay,validateAlignment,type SurveyAlignment} from '@/lib/survey-alignment';
import {inspectionText} from '@/lib/inspection-text';
import {physicalBounds,inspectionView} from '@/lib/inspection-geometry';
import SurveyPanel from './survey-panel';
import { RotateCcw, Ruler, Box, ArrowUp, X } from 'lucide-react';
type Props={siteId:string;equipment:Equipment[];baseline:Equipment[];selected:string;onSelect:(id:string)=>void;compare:boolean;siteType:SiteType;cablePlan?:CablePlan;selectedCable?:string;onCable?:(id:string)=>void};
export default function SiteViewer({siteId,equipment,baseline,selected,onSelect,compare,siteType,cablePlan,selectedCable,onCable}:Props){
 const [tooltip,setTooltip]=useState<{text:string;x:number;y:number}|null>(null);const [media,setMedia]=useState<string[]>(Object.keys(cableLabels));const host=useRef<HTMLDivElement>(null);const state=useRef({equipment,baseline,selected,onSelect,compare,cablePlan,selectedCable,onCable,media});
 const engine=useRef<{locate:(id:string)=>void;update:()=>void;view:(top:boolean)=>void;load:(buffer:ArrayBuffer,alignment?:SurveyAlignment)=>Promise<void>;align:(alignment:SurveyAlignment)=>void;clear:()=>void}|null>(null);
 const measuring=useRef(false);const [measure,setMeasure]=useState(false);const [distance,setDistance]=useState('');const [error,setError]=useState('');const [modelName,setModelName]=useState('');const [surveyOverlay,setSurveyOverlay]=useState(false);
 useEffect(()=>{
 if(!host.current)return;const parent=host.current;let renderer:THREE.WebGLRenderer;
 try{renderer=new THREE.WebGLRenderer({antialias:true});}catch{queueMicrotask(()=>setError('3D rendering is unavailable in this browser. All equipment dimensions and workflows remain available in the inventory.'));return;}
 renderer.setPixelRatio(Math.min(window.devicePixelRatio,2));renderer.setClearColor('#080f21');parent.appendChild(renderer.domElement);
 renderer.domElement.setAttribute('aria-label','3D tower model. Drag to orbit, scroll to zoom. Use inventory buttons to select equipment with the keyboard.');
 const scene=new THREE.Scene();scene.add(new THREE.AmbientLight(0xb6d5ed,2));const sun=new THREE.DirectionalLight(0xffffff,3);sun.position.set(20,30,15);scene.add(sun);
 const camera=new THREE.PerspectiveCamera(40,1,.1,300);const focus=siteType==="CORE"?2:siteType==="IBS"?6:siteType==="SMALL_CELL"?4:11;const radius=siteType==="CORE"?18:siteType==="SMALL_CELL"?20:32;camera.position.set(radius*.9,focus+12,radius);const controls=new OrbitControls(camera,renderer.domElement);controls.target.set(0,focus,0);controls.enableDamping=true;controls.maxDistance=100;controls.minDistance=2;
 const structure=createStructure(siteType);scene.add(structure);const assetsGroup=new THREE.Group();scene.add(assetsGroup);const wiring=new THREE.Group();scene.add(wiring);const ghost=new THREE.Group();scene.add(ghost);const measurements=new THREE.Group();scene.add(measurements);let imported:THREE.Group|null=null;let importedOverlay=false;
 const dispose=disposeObject;
 const clearGroup=(group:THREE.Group)=>{dispose(group);group.clear();};
 const grid=new THREE.GridHelper(70,70,0x425566,0x233441);grid.position.y=-.25;scene.add(grid);
 const equipmentMesh=(e:Equipment,isGhost=false)=>createEquipmentMesh(e,e.id===state.current.selected,isGhost);
 const update=()=>{clearGroup(assetsGroup);clearGroup(ghost);clearGroup(wiring);for(const c of state.current.cablePlan?.cables||[])if(state.current.media.includes(c.medium))wiring.add(createCableMesh(c,c.id===state.current.selectedCable));for(const d of state.current.cablePlan?.devices||[]){const mesh=createWiringFixtureMesh(d,siteId);mesh.traverse(o=>{o.userData={fixtureId:d.id,cableId:state.current.cablePlan?.cables.find(c=>c.from.assetId===d.id||c.to.assetId===d.id)?.id};});wiring.add(mesh);}for(const e of state.current.equipment){assetsGroup.add(equipmentMesh(e));}if(state.current.compare)for(const e of state.current.baseline)ghost.add(equipmentMesh(e,true));};update();
 let points:THREE.Vector3[]=[];const raycaster=new THREE.Raycaster();const pointer=new THREE.Vector2();let down={x:0,y:0};
 const start=(e:PointerEvent)=>{down={x:e.clientX,y:e.clientY};};
 const click=(e:PointerEvent)=>{if(Math.hypot(e.clientX-down.x,e.clientY-down.y)>5)return;const rect=renderer.domElement.getBoundingClientRect();pointer.set((e.clientX-rect.left)/rect.width*2-1,-(e.clientY-rect.top)/rect.height*2+1);raycaster.setFromCamera(pointer,camera);const hits=raycaster.intersectObjects(measuring.current?(imported?(importedOverlay?[imported,assetsGroup]:[imported]):[assetsGroup,structure]):[wiring,assetsGroup],true).filter(hit=>!measuring.current||!hit.object.userData.isLabel);if(!hits.length)return;if(!measuring.current){if(imported&&!importedOverlay)return;const cable=hits[0].object.userData.cableId;if(cable){state.current.onCable?.(cable);return;}const id=hits[0].object.userData.assetId;if(id)state.current.onSelect(id);return;}if(points.length===2){points=[];clearGroup(measurements);}const point=hits[0].point;points.push(point);const marker=new THREE.Mesh(new THREE.SphereGeometry(.11),new THREE.MeshBasicMaterial({color:'#ffcb78',depthTest:false}));marker.position.copy(point);measurements.add(marker);if(points.length===2){const line=new THREE.Line(new THREE.BufferGeometry().setFromPoints(points),new THREE.LineBasicMaterial({color:'#ffcb78',depthTest:false}));measurements.add(line);setDistance(`${points[0].distanceTo(points[1]).toFixed(2)} m`);}else setDistance('Select a second surface point');};
 const hover=(e:PointerEvent)=>{if(e.buttons||measuring.current||(imported&&!importedOverlay)){setTooltip(null);return;}const rect=renderer.domElement.getBoundingClientRect();pointer.set((e.clientX-rect.left)/rect.width*2-1,-(e.clientY-rect.top)/rect.height*2+1);raycaster.setFromCamera(pointer,camera);const hit=raycaster.intersectObjects([assetsGroup,wiring],true)[0];const text=hit?inspectionText(hit.object.userData,state.current.equipment,state.current.cablePlan):'';setTooltip(text?{text,x:Math.max(8,Math.min(rect.width-250,e.clientX-rect.left+14)),y:Math.max(8,Math.min(rect.height-65,e.clientY-rect.top+14))}:null);};const leave=()=>setTooltip(null);renderer.domElement.addEventListener('pointermove',hover);renderer.domElement.addEventListener('pointerleave',leave);
 renderer.domElement.addEventListener('pointerdown',start);renderer.domElement.addEventListener('pointerup',click);
 const view=(top:boolean)=>{if(imported){const box=new THREE.Box3().setFromObject(imported);if(importedOverlay)box.union(new THREE.Box3().setFromObject(assetsGroup));const center=box.getCenter(new THREE.Vector3()),size=Math.max(1,box.getSize(new THREE.Vector3()).length());camera.far=Math.max(300,size*10);camera.updateProjectionMatrix();controls.maxDistance=size*8;controls.target.copy(center);camera.position.copy(center).add(top?new THREE.Vector3(0,size*1.5,.01):new THREE.Vector3(size,size*.6,size));controls.update();return;}controls.target.set(0,top?0:focus,0);camera.position.set(top?0:radius*.9,top?42:focus+12,top?.01:radius);controls.update();};
 const clear=()=>{loadGeneration++;importedOverlay=false;if(imported){dispose(imported);scene.remove(imported);imported=null;}controls.maxDistance=100;camera.far=300;camera.updateProjectionMatrix();wiring.visible=true;structure.visible=true;grid.visible=true;assetsGroup.visible=true;ghost.visible=true;clearGroup(measurements);points=[];setDistance('');view(false);};
 let disposed=false,loadGeneration=0;
 const align=(alignment:SurveyAlignment)=>{if(!imported)throw new Error('Open the survey before aligning it.');const a=validateAlignment(alignment);applySurveyAlignment(imported,a);setSurveyMaterialOverlay(imported,a.overlay);importedOverlay=a.overlay;assetsGroup.visible=a.overlay;wiring.visible=a.overlay;ghost.visible=a.overlay;clearGroup(measurements);points=[];setDistance('');view(false);};
 const locate=(id:string)=>{const target=[...assetsGroup.children,...wiring.children].find(o=>o.userData.assetId===id||o.userData.fixtureId===id);if(!target)return;const pose=inspectionView(physicalBounds(target),camera.fov,camera.aspect);if(!pose)return;if(imported&&!importedOverlay){importedOverlay=true;setSurveyMaterialOverlay(imported,true);assetsGroup.visible=true;wiring.visible=true;setSurveyOverlay(true);}controls.target.copy(pose.target);camera.position.copy(pose.position);controls.update();};
 engine.current={locate,update,view,clear,align,load:async(buffer,alignment)=>{const request=++loadGeneration;const loaded=await loadSiteGlb(buffer);const gltf={scene:loaded.scene};if(disposed||request!==loadGeneration){dispose(gltf.scene);throw new Error("Survey load was superseded by another view.");}clear();imported=new THREE.Group();imported.add(gltf.scene);scene.add(imported);wiring.visible=false;structure.visible=false;assetsGroup.visible=false;ghost.visible=false;align(validateAlignment(alignment));}};
 const resize=()=>{const w=parent.clientWidth,h=parent.clientHeight;renderer.setSize(w,h);camera.aspect=w/Math.max(h,1);camera.updateProjectionMatrix();};const observer=new ResizeObserver(resize);observer.observe(parent);resize();
 let frame=0;const animate=()=>{frame=requestAnimationFrame(animate);controls.update();renderer.render(scene,camera);};animate();
 return()=>{disposed=true;cancelAnimationFrame(frame);observer.disconnect();controls.dispose();renderer.domElement.removeEventListener('pointermove',hover);renderer.domElement.removeEventListener('pointerleave',leave);renderer.domElement.removeEventListener('pointerdown',start);renderer.domElement.removeEventListener('pointerup',click);dispose(scene);renderer.dispose();renderer.domElement.remove();engine.current=null;};
 },[siteType,siteId]);
 useEffect(()=>{state.current={equipment,baseline,selected,onSelect,compare,cablePlan,selectedCable,onCable,media};engine.current?.update();},[equipment,baseline,selected,compare,onSelect,cablePlan,selectedCable,onCable,media]);
 const selectedConnection=cablePlan?.cables.find(c=>c.id===selectedCable);return <><p className="network-note">Microwave dishes: {cablePlan?.devices.filter(d=>d.id.startsWith("W-MW-")).map(d=>d.name).join(", ")||"None active. Blocked microwave paths have been replaced with fiber."} · Reflector, feed horn, outdoor radio and Ethernet termination are modeled.</p><div className="component-locator"><label htmlFor="locate-component">Locate component</label><select id="locate-component" defaultValue="" onChange={e=>{const id=e.target.value;if(!id)return;engine.current?.locate(id);const asset=equipment.find(a=>a.id===id);if(asset)onSelect(id);else {const connection=cablePlan?.cables.find(c=>c.from.assetId===id||c.to.assetId===id);if(connection)onCable?.(connection.id);}e.target.value="";}}><option value="">Choose equipment or wiring fixture…</option>{[...equipment,...(cablePlan?.devices||[])].map(e=><option key={e.id} value={e.id}>{e.id} · {e.name}</option>)}</select><button type="button" disabled={!selected} onClick={()=>engine.current?.locate(selected)}>Focus selected equipment</button></div><div className="twin-viewer"><details className="viewer-layer-menu"><summary>Layers</summary><div className="local-cable-toggle">{Object.keys(cableLabels).map(m=><label key={m}><Checkbox aria-label={`Show ${cableLabels[m as CableMedium]}`} checked={media.includes(m)} onCheckedChange={()=>setMedia(media.includes(m)?media.filter(x=>x!==m):[...media,m])}/>{cableLabels[m as CableMedium]}</label>)}</div></details>{tooltip&&<div className="equipment-tooltip" role="tooltip" style={{left:tooltip.x,top:tooltip.y}}>{tooltip.text}</div>}<div ref={host} className="three-host"/><div className="viewer-caption"><span className="viewer-light"/>{modelName||'Parametric site model'}<small>{modelName?'Imported geometry · units assumed metres':'Demonstration · dimensions in metres'}</small></div><div className="viewer-actions"><FullscreenButton target=".twin-viewer" label="3D site"/><button title="Equipment view" aria-label="Return to equipment view" onClick={()=>{engine.current?.clear();setModelName('');setSurveyOverlay(false);}}><RotateCcw size={18}/></button><button title="Top view" aria-label="Top view" onClick={()=>engine.current?.view(true)}><ArrowUp size={18}/></button><button aria-pressed={measure} title="Measure between surface points" onClick={()=>{measuring.current=!measure;setMeasure(!measure);setDistance('');}}><Ruler size={18}/><span>Measure</span></button></div>{error&&<div className="viewer-error" role="alert">{error}<button aria-label="Dismiss model error" onClick={()=>setError('')}><X size={16}/></button></div>}{selectedConnection&&(!modelName||surveyOverlay)&&<div className="viewer-cable-readout"><strong>{selectedConnection.label}</strong><span>{selectedConnection.from.assetId} / {selectedConnection.from.port} → {selectedConnection.to.assetId} / {selectedConnection.to.port}</span><small>{selectedConnection.cable} · {selectedConnection.length} m estimated · termination markers in gold</small></div>}<div className="viewer-footer"><span><Box size={15}/>{measure?(distance||'Select two surfaces to measure distance'):'Drag to orbit · scroll to zoom · click equipment to inspect'}</span><span>{modelName?(surveyOverlay?'SURVEY + EQUIPMENT':'SURVEY MODEL'):compare?'BASELINE OVERLAY':'DESIGN MODEL'}</span></div></div><SurveyPanel key={siteId} siteId={siteId} active={!!modelName} onOpen={async record=>{if(!engine.current)throw new Error("3D renderer unavailable. The saved survey can still be downloaded.");await engine.current.load(record.bytes.slice(0),record.alignment);setSurveyOverlay(!!record.alignment?.overlay);setModelName(record.filename);}} onAlign={alignment=>{if(!engine.current)throw new Error("3D renderer unavailable.");engine.current.align(alignment);setSurveyOverlay(alignment.overlay);}} onDesign={()=>{engine.current?.clear();setModelName("");setSurveyOverlay(false);}}/></>;
}









